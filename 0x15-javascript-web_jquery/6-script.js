$(document).ready(() => {
        $('#update_header').click(() => {
          // Update the text of the header element to "New Header!!!"
          $('header').text('New Header!!!');
        });
      });
